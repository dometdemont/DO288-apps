pipeline {
    options {
        // set a timeout of 30 minutes for this pipeline
        timeout(time: 30, unit: 'MINUTES')
    }
    agent {
      node {
        label 'master'
      }
    }
    environment {
    _ENDPOINT = "http://assistant-mad-also.2886795312-80-host05nc.environments.katacoda.com"
    }
    stages {
        stage('OpenShift project') {
            steps {
                script {
                    openshift.withCluster() {
                        openshift.withProject() {
                                echo "stage 1: using project: ${openshift.project()} in cluster ${openshift.cluster()} with name ${_NAME}"
                        }
                    }
                }
            }
        }

      stage('Default session dump empty') {
        steps {
              sh '[[ "$(curl ${_ENDPOINT}/hpe5g.html/dump)" == "[]" ]]'
          }
      }
      stage('Error on invalid session') {
        steps {
              sh '[[ $(curl ${_ENDPOINT}/unlikely.html/dump) != 0 ]]'
          }
      }

      stage('Manual approval') {
        steps {
          timeout(time: 60, unit: 'MINUTES') {
          input message: "Confirm success?"
          }
        }
      }
    }
}
